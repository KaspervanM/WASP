# WASP

## Requirements

- Nodejs
- Node package manager
- Vue.js commandline interface

## Installation

#### The Nodejs server

1. `cd server`
2. `npm install`
3. `npm start`

#### The Vue.js app

In a second terminal

1. `cd client`
2. `npm install`
3. `npm run serve`
